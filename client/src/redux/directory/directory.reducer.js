import directorySection from "../../API/CustomJson/directorySection";

const INITIAL_STATE = { directorySection };

const directoryReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default directoryReducer;
